[project]
name = "styx"
version = "0.1.0"
description = "PlutoSDR infrastructure platform — DMA, DDR, IQ capture, HIL test injection"
requires-python = ">=3.9"
dependencies = [
    "cocotb>=2.0,<3",
    "find_libpython>=0.5",
]

[tool.pytest.ini_options]
testpaths = ["fpga/test"]
